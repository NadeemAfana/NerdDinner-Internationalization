@model IEnumerable<NerdDinner.Models.Dinner>

@{
    ViewBag.Title = Resources.MyDinners;
}

<h2>@Resources.MyDinners</h2>

<ul class="upcomingdinners">
	
	@foreach (var dinner in Model)
 { 
		<li>     
			@Html.ActionLink(dinner.Title, "Details", new { id = dinner.DinnerID })
			@Resources.on 
			<strong>@dinner.EventDate.ToString("yyyy-MMM-dd")
			@dinner.EventDate.ToString("HH:mm tt")</strong>
			@Resources.atAddress 
			@dinner.Address @dinner.Country
		</li>    
 }

	@if (Model.Count() == 0)
 { 
		<li>@Resources.YouDoNotOwnDinners</li>
	}
</ul>

